package com.zensar;

import javax.persistence.EntityManager;
import javax.persistence.EntityManagerFactory;
import javax.persistence.Persistence;

import com.zensar.bean.Address;
import com.zensar.bean.BankAccount;
import com.zensar.bean.Employee;

public class App {

	public static void loadTesting() {
		// write code here to load emp object
	}

	public static void insideTesting() {
		// write code here to persist employee object

		EntityManagerFactory emf = Persistence.createEntityManagerFactory("JPAAssignment");
		EntityManager em = emf.createEntityManager();

		em.getTransaction().begin();
		
		Employee employee = new Employee("kavya", 33300.00, null, null, null);

		Address address = new Address("banglore", "560064", null);
		
		new BankAccount(0, null);
	
	}

	public static void main(String[] args) {

	}
}
